services:
  control:
    build: .
    image: dsh-testsuite:local
    ports:
      - "8090:8090"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./data:/data
      - ./web:/app/web
    environment:
      DSHTS_DATA: /data
      LOG_LEVEL: info
    restart: unless-stopped
